#include <stdio.h>
int main()
{
	char ch;
	// input a character
	printf("enter a character:");
	scanf("%c", &ch); 
	else if((ch >= 'a' && ch <= 'z
	printf("the character is an lowercase alphabet.\n");
	else if ((ch >='a' && ch <= 'z')) 
	printf("the character is a lower case alphabt.\n");
	else
	printf("the character is not an alphabet.\n");
}
